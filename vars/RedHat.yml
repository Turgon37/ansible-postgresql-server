---
# vars file for postgresql (RedHat specific)

postgresql_server__var_directory: /var/lib/pgsql

# Used to execute initdb
postgresql_server__bin_directory: '/usr/pgsql-{{ postgresql_server__version }}/bin'
